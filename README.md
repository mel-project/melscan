Melscan is a SvelteKit-Rust hybrid app.

More specifically, the `frontend` directory hosts a SvelteKit app, which talks to the Rust backend in `backend`. `dev.sh` automatically sets up an auto-refreshing server.
